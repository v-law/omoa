const db = require('../models/artModels');

const paintingController = {};

paintingController.getArt =  (req, res, next) => {
  // write code here
  
};

paintingController.getLabel = (req, res, next) => {
  // write code here
  
};

paintingController.addArt = (req, res, next) => {
  // write code here
  
};

module.exports = artController;